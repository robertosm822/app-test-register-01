class Article {
    constructor(id, title,description, url, destak_image, status, date_published) {
      (this.id = id),
        (this.title = title),
        (this.description = description),
        (this.url = url),
        (this.destak_image = destak_image),
        (this.status = status),
        (this.date_published = date_published);
    }
  }
  
  export default Article;

